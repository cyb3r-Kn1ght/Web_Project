Component la` gi` ? 

- La` nhung~ thanh` phan, chuc nang tren trang web co' kha? nang tach' nho? ra va` tai' su? dung. lai. nhieu` lan`, nhieu` noi

QUY UOC':
- Nguoi viet code trong component can` neu ra nhung function co tac dung gi de ae BE thuan. tien. trong tim` kiem'
- Chu' thich' code day` du?

